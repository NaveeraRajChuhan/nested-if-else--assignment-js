var age = +prompt('enter your age')
if (age <=100 && age >= 0   ){
    if (age >=90)
        document.write("you are too much old but if you want license so we will give you")
} elseif(age >=90){
document.write("you are too much old but if you want license so we will give you")
}


